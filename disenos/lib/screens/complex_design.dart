import 'package:flutter/material.dart';

class ComplexScreen extends StatelessWidget {
   
  const ComplexScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('ComplexScreen'),
      ),
    );
  }
}